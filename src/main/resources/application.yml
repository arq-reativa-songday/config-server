spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/arq-reativa-songday/songday-profiles
          search-paths:
      discovery:
        enabled: true
      label: main

server:
  port: 8888


#spring.cloud.config.server.git.search-paths={application}-{profile}

